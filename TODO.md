# TODO - Complete Event Platform Features

Current progress: 1/5 ✅ Plan approved, TODO created.

## Steps from approved plan:

- [x] 1. Create TODO.md with steps
- [x] 2. Create add_test_events.sql with correct columns (motdepasse, typeorganisation), 5 PUBLIE events for orga1@event.com
- [x] 3. Run SQL with psql (add_test_events_fixed.sql → 5 events + tickets for orga ID=1)
- [x] 4. Edit DataInitializer.java: Comment @Startup (avoid deploy error)
- [x] 5. Fixed IndexController + index.xhtml to use real DB events (`#{clientEvenementController.evenementsPublies}` loads on login)
- [x] 6. Flow ready: Clean/Build → Deploy → index shows DB events → login → buy → tickets → validate QR
- [x] 7. Task complete

Progress tracked here.
